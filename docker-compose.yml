version: '3'

services:
  django:
    build:
      context: .
      dockerfile: Dockerfile.django
    environment:
      - PYTHONUNBUFFERED=1

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - 8080:80
    depends_on:
      - django
